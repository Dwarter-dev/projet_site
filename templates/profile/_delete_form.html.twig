<form method="post" action="{{ path('app_profile_delete') }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete') }}">
    <button class="btn">Supprimer</button>
</form>
