{% extends 'baseAdmin.html.twig' %}

{% block title %}Langues!{% endblock %}

{% block body %}
<h1 class="pb-5">Liste des catégories</h1>

<table class="table text-center table-hover table-sm">
    <thead>
        <tr>
            <th>N°</th>
            <th>Nom</th>
        </tr>
    </thead>
    <tbody>
        {% for langues in langue %}
            <tr>
              <td class="align-middle">{{ loop.index }}</td>
              <td class="align-middle">{{ langues.nomLangue }}</td>
<<<<<<< HEAD

=======
              <td class="align-middle">
                  <a href="{{ path( 'langue_update', {'id': langue.id} ) }}" class="text-primary"><i class="fas fa-pen-square"></i></a>
                  <a href="{{ path( 'langue_delete', {'id': langue.id} ) }}" class="text-danger"><i class="fas fa-trash-alt"></i></a>
              </td>
>>>>>>> 27a8a1e6f6ab4d566c2d795ebdc02186ec60f44c
            </tr>
        {% endfor %}
    </tbody>
</table>

<div class="d-flex justify-content-end my-5">
    <a href="{{ path('langue_create') }}" class="btn btn-success">Ajouter une Langue</a>
</div>
{% endblock %}
