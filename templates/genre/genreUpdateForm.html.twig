{% extends 'baseAdmin.html.twig' %}

{% block title %}{% endblock %}

{% block body %}

  {{ form_start(genreForm) }}

  <div class="row justify-content-center">
    <div class="col-6">
      {{ form_row(genreForm.nomGenre, {'label': 'Nom du genre'}) }}
    </div>
  </div>

  <div class="col-12 d-flex justify-content-center">
    <a href="{{ path('admin_genre_index') }}" class="btn btn-danger m-3">Annuler</a>
    <button class="btn btn-success m-3">Modifier</button>
  </div>

  {{ form_end(genreForm) }}

{% endblock %}
